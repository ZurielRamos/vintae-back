# Configuración SMTP para Envío de Emails

# ===== SMTP Configuration =====
# Proveedor SMTP (Gmail, SendGrid, Mailgun, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Credenciales SMTP
SMTP_USER=tu-email@gmail.com
SMTP_PASSWORD=tu-app-password

# Información del Remitente
SMTP_FROM_NAME=Vesster
SMTP_FROM_EMAIL=noreply@vesster.com

# URL del Frontend (para links en emails)
FRONTEND_URL=https://vesster.com

# ===== Ejemplos de Configuración por Proveedor =====

# --- Gmail ---
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-email@gmail.com
# SMTP_PASSWORD=app-password  # Generar en: https://myaccount.google.com/apppasswords

# --- SendGrid ---
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=apikey
# SMTP_PASSWORD=tu-sendgrid-api-key

# --- Mailgun ---
# SMTP_HOST=smtp.mailgun.org
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=postmaster@tu-dominio.mailgun.org
# SMTP_PASSWORD=tu-mailgun-password

# --- AWS SES ---
# SMTP_HOST=email-smtp.us-east-1.amazonaws.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=tu-aws-access-key
# SMTP_PASSWORD=tu-aws-secret-key
